#version 330 core

in vec3 pixcolor;
out vec3 color;
void main(){
/*  vec2 uv = gl_TexCoord[0].xy;
  float circle_radius = 0.1;
 
  float dist =  sqrt(dot(uv, uv));
  if ( (dist > (circle_radius)) || (dist < (circle_radius)) )
    gl_FragColor = vec4(1.0f,1.0f,1.0f,1.0f);
  else 
    gl_FragColor = pixcolor;
 */   color = pixcolor;
}